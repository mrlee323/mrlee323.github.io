---
title: "데이터 조회를 개발자에게 묻지 않게 만들기: 사내 AI 스킬 구축기"
date: "2026-09-05"
category: "ai"
excerpt: "데이터를 보려면 매번 개발자를 거쳐야 하는 조직에서, 로그·DB 지식을 문서로 꺼내고 그 문서를 컨텍스트로 삼는 AI 스킬을 만든 과정. 도구보다 문서를 먼저 만든 이유와, SQL을 모르는 사람이 써도 해석이 틀어지지 않게 만든 방법을 다룹니다."
tags: ["AI", "Agent Skills", "사내도구", "데이터", "자동화"]
readingTime: 8
project: "사내 데이터 조회 AI 스킬"
---

## 왜 만들게 됐나

"이 수치 좀 볼 수 있을까요?"

이 질문이 개발자에게 오는 조직이었습니다. 문제는 질문이 많다는 게 아니라, **답할 수 있는 사람이 소수라는 것**이었습니다.

1. **로그 지식이 사람 머릿속에 있다** — 어떤 화면의 어떤 액션이 어떤 이벤트로 쌓이는지, 그 이벤트가 어느 테이블 어느 컬럼에 들어가는지를 아는 사람이 몇 명뿐이었습니다.
2. **DB 구조가 문서화돼 있지 않다** — 테이블은 많은데 실제로 어떤 조인 경로를 타야 원하는 숫자가 나오는지는 경험으로만 알 수 있었습니다.
3. **지표 정의가 흩어져 있다** — 같은 이름의 지표를 두 사람이 다르게 계산하는 일이 실제로 있었습니다. 분모를 무엇으로 두느냐에 따라 숫자가 달라지는데, 그 기준이 어디에도 안 적혀 있었습니다.

결국 병목은 도구가 아니라 **지식의 소재**였습니다. 대시보드를 더 만들어도 "그래서 이 질문에는 어떤 로그를 봐야 하죠?"는 그대로 남습니다.

## 도구보다 문서를 먼저 만들었다

병목이 지식의 소재라면, 순서는 정해져 있었습니다.

**조직에만 있는 지식은 모델이 만들어낼 수 없습니다.** 로그와 테이블의 대응 관계는 우리 조직 안에만 존재하는 사실입니다. 모델을 아무리 좋은 걸 써도 이걸 모르면 그럴듯한 거짓말을 합니다. 필요한 건 모델이 아니라 **컨텍스트**였습니다.

그리고 무엇을 만들든 **저 지식을 밖으로 꺼내는 작업은 피할 수 없습니다.** 대시보드를 만들어도, 위키를 정리해도, 도구를 붙여도 마찬가지입니다. 그래서 그걸 먼저 했습니다.

> 도구를 만들고 지식을 채우는 게 아니라, **지식을 문서로 만들고 그 문서를 컨텍스트로 쓰는 도구**를 붙인다.

이 순서로 가면 도구가 실패해도 문서는 남습니다. 실제로 이 프로젝트에서 가장 오래 걸린 건 도구 구현이 아니라 로그를 화면·액션 기준으로 전수 정리하는 일이었습니다.

## 무엇을 참고했나

구조는 [Anthropic의 Agent Skills](https://www.anthropic.com/news/skills) 방식을 그대로 따랐습니다. 스킬은 결국 **폴더 하나에 든 마크다운 문서 + 참조 파일**이고, 에이전트가 필요할 때 읽어 들이는 구조입니다.

이 방식을 고른 이유는 셋입니다.

- **버전 관리가 된다** — 스킬이 git 저장소에 있으니 지표 정의가 바뀌면 커밋으로 남습니다. "언제부터 이 계산이 바뀌었나"를 추적할 수 있습니다.
- **점진적으로 읽는다** — 전체 문서를 매번 컨텍스트에 넣지 않고 필요한 참조 파일만 열립니다. 로그 지도 전체는 꽤 길어서 이게 중요했습니다.
- **에이전트를 갈아탈 수 있다** — 특정 도구에 종속되지 않습니다. 실제로 Claude Code와 Codex 양쪽에서 왕복 검증했습니다.

데이터 연결은 [MCP(Model Context Protocol)](https://modelcontextprotocol.io/)로 붙였습니다. 여기서 한 가지 판단을 했는데, 아래에 따로 적습니다.

## 설계에서 신경 쓴 것

### 1. SQL을 모르는 사람이 쓴다

이게 가장 중요한 제약이었습니다. 쿼리를 만들어 주는 것까지는 쉽지만, **그 쿼리가 맞는지 사용자가 검증할 수 없다**는 게 위험합니다. 틀린 숫자가 그럴듯하게 나오면 아무도 못 잡습니다.

그래서 자주 쓰는 지표의 **정의와 조인 경로를 스킬 안에 규칙으로 고정**했습니다. 모델이 매번 새로 판단하지 않게 만든 겁니다.

```markdown
<!-- 스킬 안의 규칙 예시 (구조만) -->
## 전환율

- 분모: 해당 영역이 실제로 노출된 세션 (노출 이벤트 기준)
- 분자: 그 세션에서 발생한 목표 액션
- 주의: 페이지 진입 수를 분모로 쓰지 않는다.
  영역이 스크롤 아래 있으면 노출되지 않은 세션이 섞인다.
```

이렇게 적어두면 모델이 "페이지뷰로 나누면 되겠지"라고 판단할 여지가 사라집니다. **잘못된 해석을 막는 건 프롬프트가 아니라 규칙**이었습니다.

### 2. 조회 전용

SQL 실행은 읽기만 가능하게 막았습니다. 비개발 직군이 쓰는 도구에서 쓰기 권한은 얻는 것보다 잃을 게 큽니다. 검증·추출 명령을 따로 두고 둘 다 조회로만 동작하게 했습니다.

### 3. 비밀번호를 받지 않는다

초기에는 데이터 시각화 도구 계정으로 붙였는데, 이러면 **도구가 사용자 비밀번호를 알게 됩니다.** 사내 도구라도 이건 만들면 안 되는 구조라고 봤습니다.

토큰 방식으로 바꿨습니다. 사용자가 발급한 토큰만 넘기고 도구는 자격증명을 저장하지 않습니다. 권한 회수도 토큰 폐기로 끝납니다.

> 사내 도구라서 대충 해도 된다는 판단은, 그 도구가 전사로 퍼진 뒤에 비용을 청구합니다.

### 4. 묻지 않은 것을 덧붙이지 않는다

문서 원칙 중 하나였는데 실제로 효과가 컸습니다. AI가 답을 길게 하면 신뢰가 올라가는 게 아니라 **검증할 게 늘어납니다.** 물어본 것만 답하고, 도메인 정의가 필요한 순간에만 되묻도록 규칙에 넣었습니다.

## 배포는 두 경로로

사내 배포에서 의외로 시간이 걸린 부분입니다. 두 가지를 다 지원했습니다.

| 경로 | 대상 | 특징 |
|---|---|---|
| 저장소 등록 | 개발 직군 | 업데이트가 자동으로 따라옴 |
| zip 전달 → 폴더 등록 | 비개발 직군 | git을 모르는 사람도 쓸 수 있음 |

처음엔 저장소 방식만 생각했는데, **git을 안 쓰는 직군이 주 사용자**라는 걸 감안하면 이건 문턱이었습니다. 비개발자용 온보딩 문서도 한 장 따로 만들었습니다. 개발자용 README를 그대로 주면 첫 줄에서 막힙니다.

## 성과

- **전사·비개발 직군까지 사용 중**입니다. 데이터 확인에 개발자를 경유하는 단계가 없어졌습니다.
- 흩어져 있던 **로그·DB 지식이 문서로 남았습니다.** 스킬을 걷어내도 이 자산은 남습니다.
- 부수 효과로 지표 정의가 통일됐습니다. 규칙으로 적는 과정에서 "그동안 두 팀이 다르게 계산하고 있었다"는 게 드러났습니다.

솔직히 말하면 마지막 항목이 가장 컸습니다. 도구를 만들려고 문서를 쓰다 보니 **조직이 뭘 모르고 있었는지가 드러났습니다.**

## 정리

사내 AI 도구를 고민한다면 순서를 이렇게 두는 걸 권합니다.

1. **지식을 문서로 먼저 꺼낸다** — 도구가 실패해도 남는 자산
2. **판단이 갈리는 지점을 규칙으로 고정한다** — 모델이 매번 새로 해석하지 않게
3. **비개발자를 1급 사용자로 놓는다** — 배포 경로와 문서를 따로 준비
4. **자격증명을 도구가 갖지 않게 한다** — 퍼진 뒤엔 되돌리기 어렵다

모델 성능이 문제인 경우는 생각보다 적습니다. 대부분은 **컨텍스트가 없거나, 판단 기준이 안 적혀 있는 문제**였습니다.
