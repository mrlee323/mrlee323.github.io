---
title: "ChatGPT API로 React 앱에 AI 기능 추가하기"
date: "2025-12-01"
category: "ai"
excerpt: "OpenAI ChatGPT API를 React 애플리케이션에 통합하는 방법을 단계별로 설명합니다. 스트리밍 응답, 대화 히스토리 관리, 에러 처리까지 실전 예제와 함께 알아봅니다."
tags: ["AI", "ChatGPT", "OpenAI", "React", "Streaming"]
readingTime: 13
---

## ChatGPT API를 React에 통합하기

ChatGPT API를 React 앱에 통합하는 것은 생각보다 어렵지 않습니다. 이 글에서는 기본 연동부터 스트리밍, 대화 히스토리 관리, Next.js API 라우트를 통한 안전한 키 관리까지 실전에서 바로 사용할 수 있는 코드를 설명합니다.

## 기본 설정

```bash
npm install openai
```

```typescript
// lib/openai.ts
import OpenAI from 'openai';

// 서버 사이드에서만 사용
export const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});
```

> API 키는 절대 클라이언트 사이드 코드에 노출하면 안 됩니다. 반드시 서버 사이드(Next.js API 라우트 또는 Server Action)에서만 사용하세요.

## Next.js API 라우트로 안전하게 프록시

```typescript
// app/api/chat/route.ts
import { openai } from '@/lib/openai';
import type { ChatCompletionMessageParam } from 'openai/resources/chat/completions';

export async function POST(request: Request) {
  const { messages } = await request.json() as {
    messages: ChatCompletionMessageParam[];
  };

  const response = await openai.chat.completions.create({
    model: 'gpt-4o',
    messages,
    stream: true,
  });

  // ReadableStream으로 스트리밍 응답 전달
  const stream = new ReadableStream({
    async start(controller) {
      for await (const chunk of response) {
        const text = chunk.choices[0]?.delta?.content ?? '';
        if (text) {
          controller.enqueue(new TextEncoder().encode(text));
        }
      }
      controller.close();
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Transfer-Encoding': 'chunked',
    },
  });
}
```

## 대화 상태 관리 훅

```typescript
// hooks/useChat.ts
'use client';

import { useState, useCallback } from 'react';

interface Message {
  role: 'user' | 'assistant';
  content: string;
}

export function useChat() {
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  const sendMessage = useCallback(async (content: string) => {
    if (!content.trim() || isLoading) return;

    const userMessage: Message = { role: 'user', content };
    const newMessages = [...messages, userMessage];

    setMessages(newMessages);
    setInput('');
    setIsLoading(true);

    // 빈 assistant 메시지 추가 (스트리밍 중 채워질 것)
    setMessages((prev) => [...prev, { role: 'assistant', content: '' }]);

    try {
      const response = await fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ messages: newMessages }),
      });

      if (!response.body) throw new Error('No response body');

      const reader = response.body.getReader();
      const decoder = new TextDecoder();

      while (true) {
        const { done, value } = await reader.read();
        if (done) break;

        const text = decoder.decode(value);

        setMessages((prev) => {
          const updated = [...prev];
          const last = updated[updated.length - 1];
          if (last.role === 'assistant') {
            return [...updated.slice(0, -1), { ...last, content: last.content + text }];
          }
          return updated;
        });
      }
    } catch (error) {
      console.error('Chat error:', error);
      setMessages((prev) => [
        ...prev.slice(0, -1),
        { role: 'assistant', content: '죄송합니다. 오류가 발생했습니다.' },
      ]);
    } finally {
      setIsLoading(false);
    }
  }, [messages, isLoading]);

  return { messages, input, setInput, sendMessage, isLoading };
}
```

## 채팅 UI 컴포넌트

```tsx
// components/ChatInterface.tsx
'use client';

import { useRef, useEffect } from 'react';
import { useChat } from '@/hooks/useChat';

export function ChatInterface() {
  const { messages, input, setInput, sendMessage, isLoading } = useChat();
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    sendMessage(input);
  };

  return (
    <div className="flex flex-col h-[600px] border rounded-xl overflow-hidden">
      {/* 메시지 목록 */}
      <div className="flex-1 overflow-y-auto p-4 space-y-4">
        {messages.length === 0 && (
          <div className="text-center text-gray-400 mt-8">
            무엇이든 물어보세요!
          </div>
        )}

        {messages.map((msg, i) => (
          <div
            key={i}
            className={`flex ${msg.role === 'user' ? 'justify-end' : 'justify-start'}`}
          >
            <div
              className={`max-w-[80%] rounded-2xl px-4 py-2 ${
                msg.role === 'user'
                  ? 'bg-blue-500 text-white'
                  : 'bg-gray-100 text-gray-900'
              }`}
            >
              <p className="whitespace-pre-wrap">{msg.content}</p>
              {msg.role === 'assistant' && !msg.content && isLoading && (
                <span className="inline-block animate-pulse">▋</span>
              )}
            </div>
          </div>
        ))}

        <div ref={bottomRef} />
      </div>

      {/* 입력 폼 */}
      <form onSubmit={handleSubmit} className="border-t p-4 flex gap-2">
        <input
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="메시지를 입력하세요..."
          disabled={isLoading}
          className="flex-1 px-4 py-2 border rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
        <button
          type="submit"
          disabled={isLoading || !input.trim()}
          className="px-4 py-2 bg-blue-500 text-white rounded-xl disabled:opacity-50"
        >
          전송
        </button>
      </form>
    </div>
  );
}
```

## 시스템 프롬프트로 AI 역할 설정

특정 용도에 맞는 AI 어시스턴트를 만들 때는 시스템 프롬프트를 활용합니다.

```typescript
// 코드 리뷰 어시스턴트
const SYSTEM_PROMPTS = {
  codeReview: `당신은 경험 많은 프론트엔드 개발자입니다.
코드를 리뷰할 때 다음을 확인합니다:
1. TypeScript 타입 안전성
2. React 패턴과 Hook 규칙 준수
3. 성능 최적화 기회
4. 접근성 문제
5. 보안 취약점
한국어로 친절하게 설명해주세요.`,

  contentHelper: `당신은 기술 블로그 작성을 도와주는 어시스턴트입니다.
명확하고 이해하기 쉬운 한국어로 기술 내용을 설명합니다.`,
};
```

## 비용 최적화

ChatGPT API는 토큰 기반 과금이므로 효율적으로 사용해야 합니다.

```typescript
// 대화 히스토리 최대 길이 제한
function trimMessages(messages: Message[], maxTokens = 3000): Message[] {
  // 최근 10개 메시지만 유지 (간단한 전략)
  return messages.slice(-10);
}

// 또는 tiktoken 라이브러리로 정확한 토큰 계산
import { encoding_for_model } from 'tiktoken';

function countTokens(text: string): number {
  const enc = encoding_for_model('gpt-4o');
  return enc.encode(text).length;
}
```

ChatGPT API 통합은 생각보다 간단합니다. 스트리밍을 활용하면 사용자 경험이 크게 향상되고, Next.js API 라우트를 통해 API 키를 안전하게 보호할 수 있습니다.
